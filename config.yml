title: harrypra_7
description: nothing special just beginning.
template:
  repo: harrypra_7-template
  name: harrypra_7
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
